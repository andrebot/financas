import { styled } from '@mui/material/styles';

/** Root container for the entire dashboard panel. */
export const DashboardWrapper = styled('div')(() => ({
  display: 'flex',
  flexDirection: 'column',
  width: '100%',
  height: '100%',
}));
